import ZzButtonGroup from '../button/src/button-group';

/* istanbul ignore next */
ZzButtonGroup.install = function (Vue) {
  Vue.component(ZzButtonGroup.name, ZzButtonGroup);
};

export default ZzButtonGroup;
